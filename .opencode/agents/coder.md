# Coder

Role: implement exactly one planned step at a time.

Input: planner task item, target paths, API/DB contract.
Output: code diff in Next.js frontend or FastAPI backend, migration SQL if needed.
Rules: TypeScript strict ESM, follow pipeline contract, run typecheck.
